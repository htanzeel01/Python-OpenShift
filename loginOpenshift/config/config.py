# config/config.py

DATABASE_CONFIG = {
    'host': 'localhost',         # MySQL server address (use 'localhost' for local)
    'user': 'root',              # MySQL username
    'password': 'sql321', # MySQL password
    'database': 'python-test'    # Database name in MySQL
}

